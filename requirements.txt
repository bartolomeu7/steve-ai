requests>=2.31
psutil>=5.9
pyttsx3>=2.90
pytest>=8.0
sounddevice>=0.5
faster-whisper>=1.2
numpy>=1.26
customtkinter>=6.0
Pillow>=10.0
edge-tts>=6.1
pygame>=2.5
pywin32>=306
ddgs>=9.0
pyperclip>=1.8
pycaw>=20240210
comtypes>=1.2
mss>=9.0
pywebview>=5.0

openwakeword>=0.6.0

# launcher/updater/ (FASE 2 — update client): PEP 440 version comparison and
# Ed25519 public-key signature verification. Built from this same .venv (see
# launcher/README.md), so both are declared here rather than in a separate file.
packaging>=23
cryptography>=42
