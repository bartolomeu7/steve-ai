# Implementation Plan: Orb + STT + play_music

1. states.py — IDLE label
2. animation.py — breath IDLE mais vivo
3. orb.py — supersample + glow liquido
4. stt.py + config.json — base + initial_prompt
5. browser.py + main.py — PlayMusicTool
6. pytest orb/styles/browser
