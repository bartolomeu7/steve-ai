# Auditoria STEVE AI — 2026-09-19
Escopo: sistema completo EXCETO voz/STT/TTS/wake. Foco: leveza + ferramentas + pendências.

## Arquitetura (como está)
UI (CustomTkinter HUD/Workspace) → SteveApp → Orchestrator → AIRouter → Ollama (llama3.2:1b)
→ ToolManager + PermissionManager → 21 Tools
+ Memory SQLite | EventBus | Lifecycle | LearningRouter (G:\Grok\SteveLearning)
+ SecurityEngine (sempre ligado) → força SystemMonitorService (CPU/GPU/RAM/disco/rede/processos)

## Boot (voz desligada no teste)
- import main: ~0,2s
- build_app_services: ~2,0s
- 21 tools registradas
- SecurityEngine thread + SystemMonitor thread já ativos no boot

## Ferramentas (21)
| Tool | Nível | Papel |
|------|-------|-------|
| open_application | LOW | Abre app por nome |
| list_processes | LOW | Lista processos |
| check_cpu / check_ram / check_disk | LOW | Métricas pontuais (psutil) |
| list_directory / create_directory / open_file | LOW | FS básico |
| open_url | LOW | Browser padrão |
| play_music | LOW | Player interno WebView2/YouTube |
| active_window | LOW | Janela em foco |
| clipboard | MEDIUM | Ler/resumir/escrever clipboard |
| volume | LOW | Volume/mute |
| screenshot | MEDIUM | Print PNG |
| web_search | LOW | DuckDuckGo (ddgs) |
| remember_fact / forget_memory / recall_memories | LOW | Memória SQLite |
| check_security_status / list_security_findings / scan_file | LOW | Security Center |

Auxiliar: tools/youtube_resolve.py (só usado por play_music).

### Gaps / refinamentos de tools
- Sem escrita/edição/apagar arquivo (só criar pasta + abrir)
- Sem encerrar processo (só listar)
- Sem notificação Windows / toast
- Sem calendário, e-mail, WhatsApp
- check_cpu/ram/disk sobrepõem SystemMonitor (OK como tools do modelo, mas custo duplicado de conceito)
- Modelo 1b + 21 tools: risco de tool-call fraco / escolha errada — vale subset “essencial” + “avançado”
- scan_file / security tools provavelmente pouco usadas no dia a dia
- Falta tool de “abrir pasta favorita” / atalhos G: e C:\Users\HAKARI\Downloads\Steve AI
- Sem tool de status do próprio Steve (modo UI, ollama up, versão)

## Leveza — principais consumidores
1. **Orb 30 FPS + supersample 4×** (numpy 960² → Lanczos → PhotoImage) — maior custo visual idle
2. **SecurityEngine sempre ON** → chama `system_monitor.start()` no boot (intervalo 1s + processos 2s + GPU)
3. **async_bridge** poll 40ms
4. **pywebview/WebView2** ao tocar música (pesado, sob demanda — ok se lazy)
5. **.venv ~592 MB** (inclui faster-whisper/openwakeword mesmo fora do escopo de voz)
6. Remontagem HUD↔Workspace recria Orb/Chat/Input (custo ao trocar modo)
7. Lixo: `*.pre_jarvis.bak`, `_jarvis_hud_pkg*`, tarballs na raiz

## UI / produto (não-voz) — estado
- HUD Jarvis cyan + Workspace + toggle OK
- Boot: borderless fullscreen 1920×1080 + splash cyan OK
- Learning em G: OK (wired no Orchestrator)
- Escape fecha; start_minimized forçado false no boot fullscreen

## Pendências / riscos
- Docs ainda dizem “monitor só no Dashboard”, mas Security força monitor no boot (doc drift)
- Modelo `llama3.2:1b` é leve mas limitado p/ tool-calling rico
- Sem provider cloud opcional (só Ollama) — latência/qualidade amarradas ao local
- permission_auto_approve_level LOW: clipboard/screenshot pedem confirmação (bom) — UX de confirmação na GUI merece revisão
- Single-instance OK
- 584 testes coletados — boa rede de segurança

## Backlog priorizado

### P0 — impacto alto / leveza ou correção real
1. Orb idle: baixar FPS (ex. 15–20) e/ou supersample 4×→2× quando idle; 4× só em speak/listen
2. Security/Monitor: modo “leve” — não iniciar SystemMonitor até abrir Dashboard OU intervalo 1s→3–5s + pular GPU se Dashboard fechado
3. Lazy-import WebView2/play_music (não carregar webview no boot)
4. Documentar de verdade o boot (Security → Monitor) em ARCHITECTURE.md

### P1 — refinamento ferramentas + UX
5. Presets de tools: `essencial` (8–10) vs `completo` (21) configurável
6. Tools FS: `write_text_file` / `delete_path` com PermissionLevel HIGH + confirmação
7. Tool `notify` (toast Windows) e `open_path` favoritos (Steve AI, G:\Grok)
8. Tool `steve_status` (ui_mode, ollama, tools count, learning root)
9. Confirmação GUI unificada p/ MEDIUM/HIGH (clipboard, screenshot, delete)
10. Limpar `*.bak` e `_jarvis_hud_pkg*` da raiz do projeto

### P2 — polish / próximo nível
11. Routing: modelo maior só quando há tool-call complexo (mantém 1b no chat curto) — sem LangChain
12. Cache de tool specs / system prompt enxuto por modo
13. Workspace: painel projeto com atalhos reais do disco José
14. Telemetria local opcional: CPU do próprio Steve (psutil do PID) no HUD em debug
15. Empacotar deps voice em extra opcional (`requirements-voice.txt`) p/ install slim
16. Revisar `create_directory`/`open_file` path traversal + allowlist home/G:
17. HUD: desligar animação Orb quando janela não está em foco (save CPU)

## Fora de escopo (pedido)
- Voz / Azure STT / wake — não auditado em profundidade; ainda pendente Azure quando José quiser
